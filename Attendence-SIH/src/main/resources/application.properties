spring.application.name=Attendence-SIH
server.port=8080

# Running in stub mode (no DB). We'll enable these when wiring MySQL:
spring.datasource.url=jdbc:mysql://localhost:3306/attendance_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# CORS can be refined later; @CrossOrigin is used in controllers for now.

# --- Security (development only) ---
# Defining an explicit in-memory user stops Spring from generating a random password.
# Do NOT use these values in production. Move secrets to environment variables or a vault.
spring.security.user.name=admin
spring.security.user.password=fa8905b7-3214-4157-9396-9f38e7d60c98
spring.security.user.roles=ADMIN
